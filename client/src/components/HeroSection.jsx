export function HeroSection() {
  return (
    <section className="demo-hero">
      <p className="demo-kicker">Client install test</p>
      <h1>Using the published AI widget package</h1>
      <p>
        This client app imports from <code>epyc-ai-agent-widget</code>, so you
        can uninstall and reinstall the package to verify the integration.
      </p>
    </section>
  );
}
