# Root reference only. Copy the client/server examples into their folders:
#
# cp client/.env.example client/.env
# cp server/.env.example server/.env
#
# Client only stores the backend endpoint.
# Server stores OPENAI_API_KEY and calls the model.
