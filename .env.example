# Demo-only direct model call. Prefer VITE_AI_AGENT_ENDPOINT for production.
VITE_AI_AGENT_API_KEY=
VITE_AI_AGENT_MODEL=gpt-4o-mini

# Recommended production option: your own backend route that keeps model keys secret.
# It should accept { "message": string, "history": AgentMessage[] }
# and return { "reply": string }.
VITE_AI_AGENT_ENDPOINT=
